#!/usr/bin/env python

from setuptools import find_packages, setup

setup(
    name='shop',
    version='0.1',
    packages=find_packages(),
    url='',
    license='apache',
    author='dsociative',
    author_email='admin@geektech.ru',
    description='',
    install_requires=[
        'pytest'
    ],
)
